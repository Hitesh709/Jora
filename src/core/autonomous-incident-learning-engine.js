export function learnFromIncident({incident={},repair={},outcome="unknown"}={}){const type=String(incident.type||"unknown"),strategy=String(repair.strategy||"manual"),symptoms=Array.isArray(incident.symptoms)?incident.symptoms:[];const resolved=outcome==="resolved";return{version:"6.6",pattern:type,symptoms,repairStrategy:strategy,outcome,preventiveRule:resolved?{detect:type,apply:strategy}:null,learningStatus:resolved?"LEARNED":"EVIDENCE_REQUIRED"}}export default{learnFromIncident};