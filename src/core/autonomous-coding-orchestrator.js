import {RepositoryInspectionEngine} from "./repository-inspection-engine.js";import {CodeChangePlanner} from "./code-change-planner.js";import {CodeGenerationEngine} from "./code-generation-engine.js";import {TestGenerationEngine} from "./test-generation-engine.js";import {ValidationExecutionEngine} from "./validation-execution-engine.js";import {FailureDiagnosisEngine} from "./failure-diagnosis-engine.js";import {PatchRetryEngine} from "./patch-retry-engine.js";import {GitReleaseEngine} from "./git-release-engine.js";import {DeploymentVerificationEngine} from "./deployment-verification-engine.js";
export class AutonomousCodingOrchestrator{constructor(){this.version="1.70.0";this.inspector=new RepositoryInspectionEngine();this.planner=new CodeChangePlanner();this.generator=new CodeGenerationEngine();this.tests=new TestGenerationEngine();this.validator=new ValidationExecutionEngine();this.diagnoser=new FailureDiagnosisEngine();this.retry=new PatchRetryEngine();this.release=new GitReleaseEngine();this.deploy=new DeploymentVerificationEngine();}
build({task,repository={}}={}){const inspection=this.inspector.inspect({repository});const plan=this.planner.plan({task,inspection});const generated=this.generator.generate({plan});const artifacts={...generated,artifacts:generated.files||[]};const tests=this.tests.generate({artifacts:artifacts.artifacts});const validation=this.validator.validate({tests:tests.tests});const release=this.release.prepare({artifacts:artifacts.artifacts,validation});const deployment=this.deploy.verify({deployment:{target:"explicit"}});return{accepted:true,status:"AUTONOMOUS_CODING_PIPELINE_READY",version:this.version,inspection,plan,artifacts,tests,validation,release,deployment};}}
