import fs from "node:fs/promises";
import path from "node:path";
const DIR=".jora",FILE="project-portfolio.json",HISTORY="project-portfolio-history.json",VERSION="1.0";
const STATUSES=new Set(["registered","active","paused","blocked","completed","failed","archived"]);
const HEALTH_RANK={unknown:0,healthy:1,degraded:2,unhealthy:3,blocked:4};
async function readJson(file,fallback){try{return JSON.parse(await fs.readFile(file,"utf8"))}catch{return fallback}}
async function ensure(root){await fs.mkdir(path.join(root,DIR),{recursive:true})}
export function createPortfolioState({root}={}){return{version:VERSION,root,projects:[],priorities:[],capacity:{workers:1,active:0},status:"READY",updatedAt:new Date().toISOString()}}
export async function loadPortfolio(root){return readJson(path.join(root,DIR,FILE),null)}
export async function loadPortfolioHistory(root){return readJson(path.join(root,DIR,HISTORY),[])}
export async function savePortfolio(root,state){await ensure(root);await fs.writeFile(path.join(root,DIR,FILE),JSON.stringify(state,null,2),"utf8");return state}
async function record(root,event){const h=await loadPortfolioHistory(root)||[];h.push({...event,at:new Date().toISOString()});await ensure(root);await fs.writeFile(path.join(root,DIR,HISTORY),JSON.stringify(h.slice(-500),null,2),"utf8")}
export function validatePortfolioProject(project={}){const reasons=[];if(!project.id)reasons.push("project.id is required");if(!project.name)reasons.push("project.name is required");if(project.status&&!STATUSES.has(project.status))reasons.push("invalid project.status");return{valid:reasons.length===0,reasons}}
export function registerPortfolioProject(state,project){const validation=validatePortfolioProject(project);if(!validation.valid)return{...state,lastError:"PROJECT_REJECTED",validation};if((state.projects||[]).some(p=>p.id===project.id))return{...state,lastError:"PROJECT_EXISTS"};const entry={...project,status:project.status||"registered",priority:Number.isFinite(project.priority)?project.priority:50,health:project.health||"unknown",missionStatus:project.missionStatus||"unknown",updatedAt:new Date().toISOString()};return{...state,projects:[...(state.projects||[]),entry],lastError:null,updatedAt:new Date().toISOString(),status:"READY"}}
export function updatePortfolioProject(state,id,patch={}){if(!(state.projects||[]).some(p=>p.id===id))return{...state,lastError:"PROJECT_NOT_FOUND"};return{...state,projects:state.projects.map(p=>p.id===id?{...p,...patch,updatedAt:new Date().toISOString()}:p),lastError:null,updatedAt:new Date().toISOString()}}
export function rankPortfolioProjects(state){return[...(state.projects||[])].sort((a,b)=>(b.priority??50)-(a.priority??50)||(HEALTH_RANK[String(b.health||"unknown").toLowerCase()]??0)-(HEALTH_RANK[String(a.health||"unknown").toLowerCase()]??0)||String(a.updatedAt||"").localeCompare(String(b.updatedAt||"")))}
export function buildPortfolioPlan(state){const ranked=rankPortfolioProjects(state);const capacity=Math.max(0,Number.isFinite(state.capacity?.workers)?Math.floor(state.capacity.workers):1);return{version:VERSION,strategy:"priority-and-health-aware",capacity,assignments:ranked.slice(0,capacity).map((p,i)=>({slot:i+1,projectId:p.id,priority:p.priority,health:p.health,missionStatus:p.missionStatus}))}}
export async function registerProject(root,project){let state=await loadPortfolio(root)||createPortfolioState({root});const next=registerPortfolioProject(state,project);const status=next.lastError||"PROJECT_REGISTERED";state={...next,lastError:null};await savePortfolio(root,state);await record(root,{event:"project-registered",projectId:project.id,status});return{status,state,validation:status==="PROJECT_REJECTED"?next.validation:undefined}}
export async function updateProject(root,id,patch){const state=await loadPortfolio(root)||createPortfolioState({root});const next=updatePortfolioProject(state,id,patch);const status=next.lastError||"PROJECT_UPDATED";const saved={...next,lastError:null};await savePortfolio(root,saved);await record(root,{event:"project-updated",projectId:id,patch,status});return{status,state:saved}}
export async function buildPortfolioSnapshot(root){const state=await loadPortfolio(root)||createPortfolioState({root});return{status:"PORTFOLIO_READY",state,plan:buildPortfolioPlan(state),history:await loadPortfolioHistory(root)}}
export async function setPortfolioCapacity(root,{workers=1}={}){const state=await loadPortfolio(root)||createPortfolioState({root});const next={...state,capacity:{...state.capacity,workers:Math.max(0,Math.floor(Number(workers)||0))},updatedAt:new Date().toISOString()};await savePortfolio(root,next);await record(root,{event:"capacity-updated",workers:next.capacity.workers});return next}
export default{createPortfolioState,loadPortfolio,loadPortfolioHistory,savePortfolio,validatePortfolioProject,registerPortfolioProject,updatePortfolioProject,rankPortfolioProjects,buildPortfolioPlan,registerProject,updateProject,buildPortfolioSnapshot,setPortfolioCapacity}