import {buildAdaptivePlan} from "./adaptive-strategy-engine.js";
export function measureSelfImprovement({before={},after={}}={}){const metrics=["successRate","repairCycles","latencyMs","costUnits"];const delta={};for(const k of metrics)if(Number.isFinite(before[k])&&Number.isFinite(after[k]))delta[k]=after[k]-before[k];return{version:"1.0",delta,improved:(delta.successRate||0)>0||(delta.repairCycles||0)<0||(delta.latencyMs||0)<0,status:"MEASURED"}}
export function buildSelfImprovementCycle({objective,evidence={},candidates=[],before={},after={}}={}){const measurement=measureSelfImprovement({before,after});const plan=buildAdaptivePlan({objective,candidates,evidence});return{version:"1.0",measurement,plan,policy:{promoteOnlyIfVerified:true,rollbackOnRegression:true,retainEvidence:true}}}
export function validateSelfImprovementCycle(cycle={}){return{valid:Boolean(cycle.plan?.strategy&&cycle.policy?.promoteOnlyIfVerified===true),reasons:cycle.plan?.strategy?[]:["strategy is missing"]}}
export default{measureSelfImprovement,buildSelfImprovementCycle,validateSelfImprovementCycle};