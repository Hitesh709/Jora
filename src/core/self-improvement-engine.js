import {buildAdaptivePlan} from "./adaptive-strategy-engine.js";export function measureSelfImprovement({before={},after={}}={}){const metrics=["successRate","repairCycles","latencyMs","costUnits"],delta={};for(const k of metrics)if(Number.isFinite(before[k])&&Number.isFinite(after[k]))delta[k]=after[k]-before[k];const regressed=delta.successRate<0||delta.repairCycles>0||delta.latencyMs>0||delta.costUnits>0;const improved=!regressed&&(delta.successRate>0||delta.repairCycles<0||delta.latencyMs<0||delta.costUnits<0);return{version:"1.0",delta,improved,regressed,status:"MEASURED"}}export function buildSelfImprovementCycle({objective,evidence={},candidates=[],before={},after={}}={}){const measurement=measureSelfImprovement({before,after});const plan=buildAdaptivePlan({objective,candidates,evidence});return{version:"1.0",measurement,plan,policy:{promoteOnlyIfVerified:true,rollbackOnRegression:true,retainEvidence:true}}}export function validateSelfImprovementCycle(cycle={}){const reasons=[];if(!cycle.plan?.strategy)reasons.push("strategy is missing");if(cycle.policy?.promoteOnlyIfVerified!==true)reasons.push("verification promotion gate required");if(cycle.policy?.rollbackOnRegression!==true)reasons.push("rollback regression gate required");if(cycle.measurement?.regressed===true)reasons.push("measured regression detected");return{valid:reasons.length===0,reasons}}export default{measureSelfImprovement,buildSelfImprovementCycle,validateSelfImprovementCycle};