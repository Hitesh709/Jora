export function assessAutonomousRisk({action="unknown",files=[],verificationPassed=false,rollbackAvailable=false,securityFindings=[]}={}){const reasons=[];if(!verificationPassed)reasons.push("verification missing");if(!rollbackAvailable)reasons.push("rollback missing");if(securityFindings.length)reasons.push("security findings present");const protectedChange=files.some(f=>/^(.jora|test|\.github)\//.test(String(f)));if(protectedChange)reasons.push("protected infrastructure change");return{action,level:reasons.length>=2?"high":reasons.length?"medium":"low",allowed:reasons.length===0,reasons}}
export function buildRiskGate(input){const assessment=assessAutonomousRisk(input);return{status:assessment.allowed?"PASS":"BLOCK",assessment,requiredActions:assessment.reasons.map(r=>"resolve:"+r)}}
export default{assessAutonomousRisk,buildRiskGate};