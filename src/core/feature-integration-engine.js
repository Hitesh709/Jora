import fs from "node:fs/promises";
import path from "node:path";
const VERSION="1.0";
const FEATURES={search:{ui:"search",data:"records",api:"/api/search"},authentication:{ui:"authentication",data:"sessions",api:"/api/auth/session"},chat:{ui:"chat",data:"messages",api:"/api/chat"},commerce:{ui:"commerce",data:"cart",api:"/api/cart"},leaderboard:{ui:"leaderboard",data:"scores",api:"/api/leaderboard"},multiplayer:{ui:"multiplayer",data:"sessions",api:"/api/multiplayer/session"},booking:{ui:"booking",data:"bookings",api:"/api/bookings"},notifications:{ui:"notifications",data:"notifications",api:"/api/notifications"},gameplay:{ui:"gameplay",data:"game-state",api:"/api/game/state"}};
const PROTECTED=["test",".jora/acceptance.json",".jora/requirements.json",".git","node_modules"];
const safe=p=>{p=String(p||"").replace(/\\/g,"/");return p&&!p.startsWith("/")&&!p.split("/").includes("..")&&!PROTECTED.some(x=>p===x||p.startsWith(x+"/"))};
const exists=async p=>{try{await fs.access(p);return true}catch{return false}};
function detectFeatures(input={}){const text=JSON.stringify({command:input.command||"",blueprint:input.blueprint||{},desiredFeatures:input.desiredFeatures||[]}).toLowerCase();const out=new Set([...(input.desiredFeatures||[]),...(input.blueprint?.features||[])]);const rules=[["search",/search|filter|find|lookup/],["authentication",/login|sign[ -]?in|signup|register|authentication|auth/],["chat",/chat|messaging|message|conversation/],["commerce",/checkout|payment|cart|shop|store|ecommerce/],["leaderboard",/leaderboard|ranking|high score/],["multiplayer",/multiplayer|real[ -]?time|online players/],["booking",/booking|reservation|appointment/],["notifications",/notification|alert|reminder/],["gameplay",/game|gameplay|level|enemy|player|score/]];for(const [f,re] of rules)if(re.test(text))out.add(f);return [...out].filter(f=>FEATURES[f])}
export function mapFeatureToImplementation(feature,{blueprint={}}={}){const s=FEATURES[feature];if(!s)return null;return {feature,layers:["ui","data","api"],ui:{marker:'data-jora-feature="'+s.ui+'"'},data:{entity:s.data},api:{path:s.api,method:feature==="search"?"GET":"POST"},integration:blueprint.integrations||[]}}
export async function inspectFeatureIntegration(root,input={}){const features=detectFeatures(input),html=await fs.readFile(path.join(root,"src","index.html"),"utf8"),server=await fs.readFile(path.join(root,"src","index.js"),"utf8");const modules=await Promise.all(["src/modules/data-contract.js","src/modules/api-contract.js"].map(async f=>({path:f,present:await exists(path.join(root,f))})));return {version:VERSION,features,html,server,modules,mappings:features.map(f=>mapFeatureToImplementation(f,input)),missingUi:features.filter(f=>!html.includes('data-jora-feature="'+FEATURES[f].ui+'"')),missingApi:features.filter(f=>!server.includes(FEATURES[f].api))}}
function dataModule(features){return "export const featureDataContract={version:\"1.0\",entities:"+JSON.stringify(features.map(f=>FEATURES[f].data))+"};\n"}
function apiModule(features){return "export const featureApiContract={version:\"1.0\",routes:"+JSON.stringify(features.map(f=>FEATURES[f].api))+"};\n"}
function routeBlock(feature){const s=FEATURES[feature];return "    if(req.url==="+JSON.stringify(s.api)+"){\\n      res.writeHead(200,{\\"content-type\\":\\"application/json\\"});\\n      return res.end(JSON.stringify({feature:"+JSON.stringify(feature)+",status:\\"ok\\",entity:"+JSON.stringify(s.data)+"}));\\n    }\\n"}
export function buildFeatureIntegrationPlan(i={}){const f=i.features||[],creates=[];if(f.length&&!i.modules.find(x=>x.path==="src/modules/data-contract.js")?.present)creates.push({path:"src/modules/data-contract.js",content:dataModule(f),role:"feature-data-contract"});if(f.length&&!i.modules.find(x=>x.path==="src/modules/api-contract.js")?.present)creates.push({path:"src/modules/api-contract.js",content:apiModule(f),role:"feature-api-contract"});const apiPatches=(i.missingApi||[]).map(x=>({feature:x,path:"src/index.js",needle:'    if(req.url==="/health")',insert:routeBlock(x)}));return {version:VERSION,strategy:f.length?"cross-layer-feature-integration":"no-change",features:f,mappings:i.mappings||[],creates,apiPatches,steps:["map","data","api","ui","regression"],guardrails:["Preserve existing behavior.","Never modify tests or acceptance artifacts.","Only add routes justified by requested features.","Rollback all cross-layer changes on regression."]}}
export function validateFeatureIntegrationPlan(p={}){const reasons=[];for(const x of [...(p.creates||[]),...(p.apiPatches||[])]){if(!safe(x.path))reasons.push("unsafe path: "+x.path);if(x.content!==undefined&&typeof x.content!=="string")reasons.push("invalid content: "+x.path)}return {valid:!reasons.length,reasons}}
export async function applyFeatureIntegration(root,plan,{runTests}={}){const v=validateFeatureIntegrationPlan(plan);if(!v.valid)return {status:"REJECTED",validation:v};const originals=new Map(),created=[];try{for(const x of plan.creates||[]){const p=path.join(root,x.path);if(await exists(p))continue;await fs.mkdir(path.dirname(p),{recursive:true});await fs.writeFile(p,x.content,"utf8");created.push(x.path)}for(const x of plan.apiPatches||[]){const p=path.join(root,x.path),old=await fs.readFile(p,"utf8");if(!originals.has(p))originals.set(p,old);if(!old.includes(x.needle))throw new Error("API insertion point missing: "+x.path);await fs.writeFile(p,old.replace(x.needle,x.insert+x.needle),"utf8")}const tests=typeof runTests==="function"?await runTests(root):null;if(tests&&!tests.passed)throw new Error("regression failed after cross-layer integration");return {status:"FEATURES_INTEGRATED",integrated:true,created,patched:(plan.apiPatches||[]).map(x=>x.path),tests}}catch(error){for(const [p,c] of originals)try{await fs.writeFile(p,c,"utf8")}catch{}for(const f of created)try{await fs.rm(path.join(root,f),{force:true})}catch{}return {status:"ROLLED_BACK",integrated:false,error:error.message}}}
export async function persistFeatureIntegrationReport(root,report){await fs.mkdir(path.join(root,".jora"),{recursive:true});await fs.writeFile(path.join(root,".jora","feature-integration.json"),JSON.stringify(report,null,2),"utf8");return report}
export async function runFeatureIntegrationLoop(root,input={}){const inspection=await inspectFeatureIntegration(root,input),plan=buildFeatureIntegrationPlan(inspection);if(!plan.creates.length&&!plan.apiPatches.length)return {version:VERSION,status:"NO_INTEGRATION_NEEDED",integrated:false,inspection,plan};const result=await applyFeatureIntegration(root,plan,{runTests:input.runTests});return {version:VERSION,status:result.status,integrated:result.integrated,inspection,plan,result}}
export default {mapFeatureToImplementation,inspectFeatureIntegration,buildFeatureIntegrationPlan,validateFeatureIntegrationPlan,applyFeatureIntegration,persistFeatureIntegrationReport,runFeatureIntegrationLoop};