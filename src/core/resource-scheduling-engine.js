import fs from "node:fs/promises";import path from "node:path";
const DIR=".jora",FILE="resource-scheduler.json",HISTORY="resource-scheduler-history.json",VERSION="1.0",DEFAULT_LEASE_TTL_MS=15*60*1000;
async function readJson(f,d){try{return JSON.parse(await fs.readFile(f,"utf8"))}catch{return d}}
async function ensure(r){await fs.mkdir(path.join(r,DIR),{recursive:true})}
export function createResourceSchedulerState({root,workers=1,leaseTtlMs=DEFAULT_LEASE_TTL_MS}={}){return{version:VERSION,root,workers:Math.max(0,Math.floor(Number(workers)||0)),leaseTtlMs:Math.max(1000,Number(leaseTtlMs)||DEFAULT_LEASE_TTL_MS),leases:[],queue:[],status:"READY",updatedAt:new Date().toISOString()}}
export async function loadResourceScheduler(root){return readJson(path.join(root,DIR,FILE),null)}
export async function loadResourceSchedulerHistory(root){return readJson(path.join(root,DIR,HISTORY),[])}
export async function saveResourceScheduler(root,state){await ensure(root);await fs.writeFile(path.join(root,DIR,FILE),JSON.stringify(state,null,2));return state}
async function record(root,event){const h=await loadResourceSchedulerHistory(root)||[];h.push({...event,at:new Date().toISOString()});await ensure(root);await fs.writeFile(path.join(root,DIR,HISTORY),JSON.stringify(h.slice(-500),null,2))}
export function enqueueResourceTask(state,task){if(!task?.id||!task.projectId)return{status:"TASK_REJECTED",state};if((state.queue||[]).some(t=>t.id===task.id)||(state.leases||[]).some(t=>t.taskId===task.id))return{status:"TASK_EXISTS",state};const entry={...task,priority:Number.isFinite(task.priority)?task.priority:50,status:"queued",queuedAt:new Date().toISOString()};return{...state,queue:[...(state.queue||[]),entry],updatedAt:new Date().toISOString(),status:"READY"}}
export function allocateResourceTasks(state,now=Date.now()){const current=Number.isFinite(now)?now:Date.now();const ttl=Math.max(1000,Number(state.leaseTtlMs)||DEFAULT_LEASE_TTL_MS);const active=(state.leases||[]).filter(lease=>!lease.expiresAt||new Date(lease.expiresAt).getTime()>current);const capacity=Math.max(0,Number.isFinite(state.workers)?Math.floor(state.workers):0),slots=Math.max(0,capacity-active.length);const queue=[...(state.queue||[])].sort((a,b)=>(b.priority??50)-(a.priority??50));const selected=queue.slice(0,slots),ids=new Set(selected.map(t=>t.id));const nowIso=new Date(current).toISOString();const leases=selected.map((t,i)=>({leaseId:t.id+":"+current+":"+i,taskId:t.id,projectId:t.projectId,status:"leased",worker:i+1,leasedAt:nowIso,expiresAt:new Date(current+ttl).toISOString()}));return{...state,queue:queue.filter(t=>!ids.has(t.id)),leases:[...active,...leases],updatedAt:nowIso}}
export function releaseResourceTask(state,taskId,status="completed"){const found=(state.leases||[]).some(l=>l.taskId===taskId);return{...state,leases:(state.leases||[]).filter(l=>l.taskId!==taskId),updatedAt:new Date().toISOString(),lastRelease:{taskId,status,found,at:new Date().toISOString()}}}
export function buildSchedulingDecision(state,now=Date.now()){const next=allocateResourceTasks(state,now);return{status:"SCHEDULED",assignments:next.leases.filter(l=>!(state.leases||[]).some(x=>x.taskId===l.taskId)),state:next}}
export async function scheduleResources(root,tasks=[],options={}){let state=await loadResourceScheduler(root)||createResourceSchedulerState({root,workers:options.workers??1,leaseTtlMs:options.leaseTtlMs});if(options.leaseTtlMs!==undefined)state={...state,leaseTtlMs:Math.max(1000,Number(options.leaseTtlMs)||DEFAULT_LEASE_TTL_MS)};for(const task of tasks){const r=enqueueResourceTask(state,task);if(r.status!=="TASK_REJECTED"&&r.status!=="TASK_EXISTS")state=r}const decision=buildSchedulingDecision(state);state=decision.state;await saveResourceScheduler(root,state);await record(root,{event:"resources-scheduled",assignments:decision.assignments});return{status:"SCHEDULED",state,assignments:decision.assignments}}
export default{createResourceSchedulerState,loadResourceScheduler,loadResourceSchedulerHistory,saveResourceScheduler,enqueueResourceTask,allocateResourceTasks,releaseResourceTask,buildSchedulingDecision,scheduleResources}