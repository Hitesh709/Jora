export function compileAutonomousGoal(input={}){const request=String(input.request||input.objective||"").trim();const constraints=Array.isArray(input.constraints)?input.constraints:[];const acceptance=Array.isArray(input.acceptanceCriteria)?input.acceptanceCriteria:[];const type=/game|gameplay|arcade|puzzle/i.test(request)?"game":/api|backend|service/i.test(request)?"service":"application";return{version:"6.1",request,goal:request||"build a working product",type,constraints,acceptanceCriteria:acceptance,successConditions:acceptance.length?acceptance:["build","test","verify","deliver"]}}