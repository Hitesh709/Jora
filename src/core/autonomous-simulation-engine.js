export function createSimulationScenario({id,action,input={},expected={}}={}){return{id:id||"SIM-"+Date.now(),action,input,expected,status:"planned"}}function stable(value){if(value===null||typeof value!=="object")return value;if(Array.isArray(value))return value.map(stable);return Object.keys(value).sort().reduce((o,k)=>(o[k]=stable(value[k]),o),{})}export async function runSimulation(scenario,{executor=null}={}){if(typeof executor!=="function")return{status:"SIMULATION_BLOCKED",reason:"executor is required"};try{const actual=await executor(scenario);return{status:"SIMULATED",scenario,result:actual,matched:JSON.stringify(stable(actual))===JSON.stringify(stable(scenario.expected))}}catch(error){return{status:"SIMULATION_FAILED",scenario,error:error.message}}}export function gateSimulation(result,{requireMatch=true}={}){const allowed=result?.status==="SIMULATED"&&(!requireMatch||result.matched===true);return{allowed,status:allowed?"PASS":"BLOCK"}}export default{createSimulationScenario,runSimulation,gateSimulation};