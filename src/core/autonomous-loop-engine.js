export async function runAutonomousLoop({steps=[],execute=async()=>({success:true}),maxCycles=3}={}){const limit=Math.max(1,Math.floor(Number(maxCycles)||1));if(!Array.isArray(steps)||steps.length===0)return{version:"6.3",status:"BLOCKED",cycles:0,history:[],reason:"steps are required"};const history=[];for(let cycle=1;cycle<=limit;cycle++){let failed=false;for(const step of steps){let result;try{result=await execute(step,cycle)}catch(error){result={success:false,error:error.message}}history.push({cycle,step:step.id||step,result});if(!result?.success){failed=true;break}}if(!failed)return{version:"6.3",status:"COMPLETED",cycles:cycle,history}}return{version:"6.3",status:"EXHAUSTED",cycles:limit,history}}