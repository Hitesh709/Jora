# Jora Production Roadmap

Jora is executing a 10,000-task AI Evolution OS program.

## Current milestone

**Milestone 1 — Production vertical slice**

Requirement → Planner → Task Graph → Agent Runtime → Code Master → Sandbox → Tests → Evaluator → Versioned Result

## Phase allocation

See `docs/AI_EVOLUTION_OS.md` for the complete 20-phase allocation.

## Execution policy

Codex/agents should execute tasks in dependency-aware batches, verify acceptance criteria, run tests, update state, and commit working increments.

Do not claim completion without evidence.

## Task registry

The detailed 10,000-task workbook is maintained as a project planning artifact. The repository should maintain the executable task registry separately so implementation state remains version-controlled and machine-readable.
