import test from "node:test"; import assert from "node:assert/strict"; import {ResearchLoop} from "../src/core/research-loop.js"; import {MutationStrategyEngine} from "../src/core/mutation-strategy-engine.js"; import {EvolutionScheduler} from "../src/core/evolution-scheduler.js";
test("creates next autonomous research plan",()=>{const r=new ResearchLoop({strategyEngine:new MutationStrategyEngine(),scheduler:new EvolutionScheduler()}).planNext({champion:{version:"v1"},evaluation:{dimensions:{security:.7,latency:.9}},generation:1});assert.equal(r.strategy.strategy,"harden-security");assert.equal(r.baselineVersion,"v1");});