import test from "node:test";import assert from "node:assert/strict";import {AutonomousCodingOrchestrator} from "../src/core/autonomous-coding-orchestrator.js";
test("v1.61-v1.70 builds the coding pipeline",()=>{const r=new AutonomousCodingOrchestrator().build({task:{id:"T1",title:"Implement API",description:"Build endpoint"}});assert.equal(r.status,"AUTONOMOUS_CODING_PIPELINE_READY");assert.equal(r.version,"1.70.0");assert.ok(r.artifacts.artifacts.length);assert.equal(r.release.status,"RELEASE_PREPARED");assert.equal(r.deployment.status,"DEPLOYMENT_VERIFICATION_PLANNED");});
